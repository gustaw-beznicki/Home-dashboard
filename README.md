# Review screenshots for PR #14

Captured with `node scripts/screenshot-pr.mjs` against `npm run dev-no-auth` and
the local seed. This is an orphan branch holding nothing but these images, so the
1.5 MB never enters `main`'s history.

**Delete this branch once PR #14 is merged or closed.**
